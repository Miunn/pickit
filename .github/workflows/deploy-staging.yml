name: Staging CD Flow

on:
  push:
    branches: [staging]

jobs:
  deploy:
    uses: ./.github/workflows/deploy-cloud-run.yml
    with:
      service: echomori-staging
      environment_name: staging
      config_file: infra/cloudrun.staging.yaml
    secrets: inherit
